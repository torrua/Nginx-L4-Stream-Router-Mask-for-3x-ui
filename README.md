<div align="center">

<img src="logo.webp" alt="3X-UI Mask Logo" width="130" />

# Личный VPN на своём сервере
### 3X-UI и Nginx с маскировкой под сайт

Надёжный персональный VPN на VPS, устойчивый к блокировкам операторов связи.  
Работает на смартфонах (iOS, Android), компьютерах (Windows, macOS) и роутерах.

[Инструкция для новичков](docs/BEGINNER_GUIDE.md) • [Техническая архитектура](docs/ARCHITECTURE.md) • [Быстрый старт](#-пошаговая-установка-3-этапа)

[![Версия](https://img.shields.io/badge/версия-v6.5.3-blue?style=flat-square)](https://github.com/torrua/Nginx-L4-Stream-Router-Mask-for-3x-ui)
[![ОС](https://img.shields.io/badge/ОС-Ubuntu%20%7C%20Debian-informational?style=flat-square)](docs/BEGINNER_GUIDE.md#1-сервер-vps)
[![Лицензия](https://img.shields.io/badge/лицензия-MIT-success?style=flat-square)](LICENSE)

</div>

---

## 📌 Почему этот VPN не блокируют

Обычные протоколы (WireGuard, OpenVPN) легко вычисляются и блокируются провайдерами по характерным признакам трафика.

В этом проекте защита устроена иначе:

1. **Сервер выглядит как обычный сайт.** Снаружи это сайт компании с настоящим SSL-сертификатом.
2. **Один порт 443 для всего.** И сайт, и VPN работают на стандартном HTTPS-порту `443`. Провайдер видит только обычные зашифрованные веб-сессии.
3. **Nginx распределяет запросы:**
   * Открыли сайт в браузере или пришёл бот-сканер → Nginx показывает веб-сайт.
   * Подключилось приложение с вашим VPN-ключом → Nginx сразу передаёт трафик ядру Xray, и у вас работает интернет.
   * Открыли секретный адрес панели управления → загружается веб-интерфейс 3X-UI.
4. **Защита для мобильных сетей (AmneziaWG):**
   * В пакеты добавляется случайный шум (Junk), поэтому системы ТСПУ сотовых операторов (МТС, Билайн, Мегафон, Т2) не могут распознать протокол WireGuard.

---

## 📚 Документация

| Руководство | Описание |
| :--- | :--- |
| 🚀 **[Инструкция для новичков](docs/BEGINNER_GUIDE.md)** | Пошаговый запуск с нуля: аренда VPS и домена, укрепление ОС (`secure-vps.sh`), настройка Nginx (`setup_mask.sh`) и подключение устройств. |
| ⚙️ **[Техническое описание архитектуры](docs/ARCHITECTURE.md)** | Полная инженерная документация: схема Nginx L4 Stream, ручная настройка инбаундов 3X-UI, правила UFW от сканирования, Anti-Loop порт 9443, тюнинг буферов HTTP/2 xHTTP, диагностика (Health Check) и бэкапы. |
| 🎛️ **[Автонастройка 3X-UI (`configure_3xui.sh`)](#-шаг-3-создание-готовых-подключений-configure_3xuish)** | Скрипт, который сам создаёт готовые подключения в панели с правильными параметрами. |

---

## ⚡ Пошаговая установка (3 этапа)

### Подготовка: VPS и домен
1. Арендуйте виртуальный сервер (VPS) с **Ubuntu 22.04 / 24.04** или **Debian 11 / 12**.
2. Привяжите домен к IP сервера (A-запись в DNS).  
   *Если используете Cloudflare: обязательно включите режим **DNS Only (Серое облако)**.*

---

### Шаг 1. Защита сервера, ускорение TCP BBR и установка 3X-UI (`secure-vps.sh`)
Подключитесь к серверу по SSH под пользователем `root` и выполните:

```bash
curl -sSLO https://raw.githubusercontent.com/torrua/Nginx-L4-Stream-Router-Mask-for-3x-ui/main/secure-vps.sh && sudo bash secure-vps.sh
```
*Что делает скрипт:*
* Включает алгоритм **TCP BBR** (увеличивает скорость VPN в 2–3 раза и снижает задержки).
* Закрывает лишние порты и настраивает файрвол UFW.
* Переносит порт SSH для защиты от ботнет-сканеров.
* Устанавливает чистую панель **3X-UI**.

---

### Шаг 2. Настройка Nginx, SSL-сертификатов и маскировки (`setup_mask.sh`)
Выполните команду:

```bash
curl -sSLO https://raw.githubusercontent.com/torrua/Nginx-L4-Stream-Router-Mask-for-3x-ui/main/setup_mask.sh && sudo bash setup_mask.sh
```
Скрипт запросит ваш домен, email для Let's Encrypt и секретные пути для панели. После этого он выпустит SSL-сертификаты, развернёт сайт-маскировку и свяжет Nginx с 3X-UI.

---

### Шаг 3. Создание готовых подключений (`configure_3xui.sh`)
Запустите скрипт автонастройки:

```bash
curl -sSL https://raw.githubusercontent.com/torrua/Nginx-L4-Stream-Router-Mask-for-3x-ui/main/configure_3xui.sh | sudo bash
```
Скрипт создаст готовые профили: **AmneziaWG** (для сотовых сетей), **VLESS Reality** (для дома и ПК) и **VLESS xHTTP**.

Готово! Откройте панель в браузере (`https://ваш-домен/секретный_путь/`), добавьте пользователя и отсканируйте QR-код в приложении на телефоне.

---

## 💡 Какой протокол когда использовать

| Протокол | Для чего подходит | Как работает |
| :--- | :--- | :--- |
| **AmneziaWG (v3.1 / v2.0)** | Смартфоны на мобильном интернете (4G/5G) и роутеры Keenetic/OpenWrt | Маскирует заголовки случайными данными (Junk), фиксирует MTU 1280 (без зависаний пакетов), использует подсеть `/22` (до 1022 клиентов) и быстрый DNS от Control D. |
| **VLESS Reality** | Домашний Wi-Fi, компьютеры и ноутбуки | Скорость до 1 Гбит/с, минимальная нагрузка на процессор и аккумулятор устройства, маскировка под TLS 1.3. |
| **VLESS xHTTP** | Сети с жёсткой фильтрацией (офисы, публичный Wi-Fi) | Работает поверх чистого HTTP/2 (H2C) и шифруется `vlessenc`. Трафик неотличим от обычной загрузки сайтов. |
| **Hysteria 2** | Нестабильные линии с потерями пакетов | Работает по UDP (QUIC/HTTP/3) с протоколом BBR для разгона скорости. |

---

## 📱 Приложения для устройств

| Устройство | Рекомендуемые программы | Как настроить |
| :--- | :--- | :--- |
| **iPhone / iPad** | **Happ Proxy**, **FoXray**, **Streisand** | Открыть приложение → отсканировать QR-код из панели 3X-UI |
| **Android** | **v2rayNG**, **Happ Proxy**, **NekoBox** | Скопировать ссылку `vless://` → импортировать из буфера или по QR |
| **Windows** | **v2rayN**, **Happ**, **Flclash** | Скопировать ссылку → нажать `Ctrl + V` → включить системный прокси |
| **macOS** | **FoXray**, **V2RayXS**, **Karing** | Добавить подключение по ссылке или QR-коду |
| **Роутеры Keenetic** | Встроенный клиент WireGuard / AmneziaWG | Скачать `.conf` файл AmneziaWG в панели и загрузить в роутер |

---

## 🛠️ Автоматический запуск для сисадминов (CLI)

Скрипт `setup_mask.sh` поддерживает ключи командной строки и файл конфигурации:

```bash
# Запуск с параметрами
sudo ./setup_mask.sh -y -d mydomain.online -m admin@mydomain.online -p /my-panel/ -s /my-sub/ -f 1

# Запуск через файл .env
cp setup_mask.env.example setup_mask.env
nano setup_mask.env
sudo ./setup_mask.sh -c setup_mask.env -y
```

---

## 📄 Лицензия
Проект распространяется под свободной лицензией **MIT**.\n